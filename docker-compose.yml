version: "3.9"

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: portfolio-jessy
    restart: unless-stopped
    ports:
      - "8052:8052"
    environment:
      - NODE_ENV=production
